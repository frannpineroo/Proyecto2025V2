@page "/adminUsuarios"
@inject IHttpServicio http



<div class="container-fluid py-4 text-light bg-dark">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h2>Administración de Usuarios</h2>
        
    </div>

    <div class="btn-group mb-3">
        <button class="btn btn-danger active">Todos</button>
        <button class="btn btn-outline-secondary">Activos</button>
        <button class="btn btn-outline-secondary">Inactivos</button>
    </div>

    <div class="mb-3">
        <input type="text" class="form-control bg-secondary text-white" placeholder="Buscar usuarios...">
    </div>

    <table class="table table-dark table-hover align-middle">
        <thead>
            <tr>
                <th>Usuario</th>
                <th>Email</th>
                <th>Rol</th>
                <th>Estado</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
            
            
        </tbody>
    </table>
    <br/>
    <br />
    <div class="d-flex justify-content-between align-items-center">
     
        <nav>
            <ul class="pagination pagination-sm mb-0">
                <li class="page-item disabled"><a class="page-link bg-dark text-white">‹</a></li>
                <li class="page-item active"><a class="page-link bg-primary border-primary" href="#">1</a></li>
                <li class="page-item"><a class="page-link bg-dark text-white" href="#">2</a></li>
                <li class="page-item"><a class="page-link bg-dark text-white" href="#">3</a></li>
                <li class="page-item"><a class="page-link bg-dark text-white" href="#">›</a></li>
            </ul>
        </nav>
    </div>
</div>

<style>
    body {
    background-color: #111827;
    font-family: 'Segoe UI', sans-serif;
    }
    .table-dark {
    --bs-table-bg: #1f2937;
    --bs-table-hover-bg: #374151;
    color: #e5e7eb;
    }
    .form-control::placeholder {
    color: #9ca3af;
    }
    .btn-outline-light:hover {
    background-color: #2563eb;
    border-color: #2563eb;
    }
</style>

@code{
    ListaChatDTO chats = new ListaChatDTO();
    string Mensaje = "";
    private async Task LeerPaises()
    {
        
        var resp = await http.Get<List<ListaChatDTO>>("api/Chat");
        if (!resp.Error)
        {
            //chats = resp.Respuesta;
        }
        else
        {
            Mensaje = resp.ObtenerError();
        }
    }
}